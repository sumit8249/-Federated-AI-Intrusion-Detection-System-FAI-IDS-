# Production Serverless Deployment Requirements (Vercel / Cloud Run / Lambda)
# For full local PyTorch / Flower / Streamlit development, see requirements-full.txt

fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0
numpy>=1.24.0
requests>=2.31.0
